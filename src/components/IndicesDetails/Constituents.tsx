import { formatNumber } from "@/utils";
import styles from "./IndicesDetails.module.scss";

const IndicesConstituents = ({ data }: any) => {
  return (
    <>
      <h2 className={styles.heading}>Nifty 50 Constituents</h2>
    </>
  );
};

export default IndicesConstituents;
