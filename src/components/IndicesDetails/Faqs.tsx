import { formatNumber } from "@/utils";
import styles from "./IndicesDetails.module.scss";

const IndicesFaqs = ({ data }: any) => {
  return (
    <>
      <h2 className={styles.heading}>Frequently Asked Questions</h2>
    </>
  );
};

export default IndicesFaqs;
